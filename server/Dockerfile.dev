FROM node:20-alpine
WORKDIR /app

# Install dependencies first (cached layer)
COPY package*.json ./
RUN npm install

# Copy source (will be overridden by bind mount in dev)
COPY . .

EXPOSE 5000

# nodemon watches for changes and restarts via ts-node
CMD ["npm", "run", "dev"]
